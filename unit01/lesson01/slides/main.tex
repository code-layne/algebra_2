%! Linear Functions: Slope, Forms & Graphing — Unit 1, Lesson 1.1 — Slides (Beamer)
% Deck follows the lesson's gradual-release flow: hook → warm-up → guided notes (I do / we do) →
% group activity → debrief → close & homework.
\documentclass[aspectratio=169, 11pt]{beamer}
\usepackage{algebra2-beamer}   % provides \forestheader{} and \sectionlabel[color]{}
% Standard coordinate grid + axes: {xmin}{xmax}{ymin}{ymax}
\newcommand{\lgrid}[4]{%
  \draw[step=1, linegray!45, thin] (#1,#3) grid (#2,#4);
  \draw[->, thick, charcoal] (#1,0)--(#2,0) node[below right, font=\tiny]{$x$};
  \draw[->, thick, charcoal] (0,#3)--(0,#4) node[left, font=\tiny]{$y$};}
% Gym cost grid: climbs n on 0..8, cost C on 0..30.
\newcommand{\gymgrid}{%
  \draw[xstep=1, ystep=3, linegray!45, thin] (0,0) grid (8,30);
  \draw[->, thick, charcoal] (0,0)--(8.7,0) node[right, font=\tiny]{$n$};
  \draw[->, thick, charcoal] (0,0)--(0,32) node[above, font=\tiny]{$C$};
  \foreach \x in {2,4,6,8} \node[charcoal, font=\tiny, below] at (\x,0){\x};
  \foreach \y in {6,18,30} \node[charcoal, font=\tiny, left] at (0,\y){\y};}

\begin{document}

% TITLE SLIDE (CourseName is not defined in beamer, so the name is literal)
{
\setbeamercolor{background canvas}{bg=forest}
\begin{frame}[plain]
  \vspace{2.8cm}\hspace{0.55cm}%
  \begin{minipage}{0.9\textwidth}
    {\color{goldacc}\bfseries\small LESSON 1.1}\\[0.5cm]
    {\color{white}\bfseries\LARGE Linear Functions: Slope, Forms \& Graphing}\\[1.2cm]
    {\color{forestmid}\small Algebra 2~$\cdot$~Unit 1: Linear Functions}
  \end{minipage}
\end{frame}
}

% LEARNING TARGETS
\begin{frame}[t, plain]
  \forestheader{Today's targets}
  \sectionlabel{What you will be able to do}
  \begin{itemize}\setlength{\itemsep}{6pt}
    \item Find the \textbf{slope} from a graph or from \emph{any} two points.
    \item Write a line in \textbf{slope-intercept}, \textbf{point-slope}, and \textbf{standard}
          form --- even when nobody tells you the starting value.
    \item Recognise when two equations that \emph{look} different are the same line.
    \item Describe a line as a transformation of the parent $y=x$; identify \textbf{parallel},
          \textbf{perpendicular}, horizontal, and vertical.
  \end{itemize}
  \vspace{4pt}
  \begin{block}{How today runs}
    Warm-up (5) $\to$ notes together (15) $\to$ group activity (25) $\to$ debrief (10) $\to$ close (5).
  \end{block}
\end{frame}

% WARM-UP
\begin{frame}[t, plain]
  \forestheader{Warm-Up: three things you already know}
  \sectionlabel{5 minutes --- alone, then check with a neighbour}
  \begin{enumerate}\setlength{\itemsep}{7pt}
    \item \textbf{Count the steps.} From $(1,-1)$ to $(4,3)$: how far right, how far up? So how far
          up per \emph{one} step right? Where does the line cross the vertical axis?
    \item \textbf{Solve for $y$.} \; $2x+3y=12$ \quad and \quad $5x-y=8$.
    \item \textbf{Distribute, then tidy.} \; $y-5=3(x-2)$ \quad and \quad $y+1=-2(x+4)$.
  \end{enumerate}
  \vspace{6pt}
  \begin{block}{Hold on to this}
    In 2 and 3 you started with equations that looked nothing alike and ended with something much
    easier to read.
  \end{block}
\end{frame}

% HOOK
\begin{frame}[t, plain]
  \forestheader{Hook: the bill that hides the monthly fee}
  \sectionlabel[goldacc]{Work backwards}
  \begin{itemize}\setlength{\itemsep}{5pt}
    \item Two months of bills: \textbf{4~GB cost \$52}, \textbf{10~GB cost \$70}.
    \item $6$ more GB for \$$18$ more \; $\Rightarrow$ \; \$$3$ per GB.
    \item So the flat fee is $52 - 4(3) = \$40$.
  \end{itemize}
  \vspace{6pt}
  \begin{block}{The point}
    Nobody handed you the starting value --- you \emph{worked back} to it. Today: build the equation
    of a line from a graph, from two points, or from a slope and \emph{any} point at all.
  \end{block}
\end{frame}

% NOTES 1 — SLOPE
\begin{frame}[t, plain]
  \forestheader{Notes 1: slope from any two points}
  \sectionlabel{Same order, top and bottom}
  \begin{columns}[T]
    \begin{column}{0.54\textwidth}
      \[ m=\frac{\Delta y}{\Delta x}=\frac{y_2-y_1}{x_2-x_1} \]
      The phone plan gives the points $(4,52)$ and $(10,70)$:
      \[ m=\frac{70-52}{10-4}=\frac{18}{6}=3 \]
      \$3 per GB. \; \emph{Any} two points work --- the rate is constant (Lesson 1.0).
    \end{column}
    \begin{column}{0.42\textwidth}
      \centering
      \begin{tikzpicture}[scale=0.40]
        \lgrid{-3}{5}{-6}{6}
        \draw[very thick, forest] (-2,-6)--(4,6);
        \draw[thick, navy, dashed] (1,0)--(2,0)--(2,2);
        \node[navy, font=\tiny] at (1.5,-0.8){run};
        \node[navy, font=\tiny, right] at (2.1,1){rise};
        \fill[charcoal] (0,-2) circle (4pt);
        \fill[charcoal] (3,4) circle (4pt);
      \end{tikzpicture}
    \end{column}
  \end{columns}
\end{frame}

% NOTES 2 — THREE FORMS
\begin{frame}[t, plain]
  \forestheader{Notes 2: three ways to write one line}
  \sectionlabel{The forms}
  \begin{itemize}\setlength{\itemsep}{5pt}
    \item \textbf{Slope-intercept} $y=mx+b$ --- hands you the rate and the starting value.
    \item \textbf{Point-slope} $y-y_1=m(x-x_1)$ --- built from a slope and \emph{any} point.
    \item \textbf{Standard} $Ax+By=C$ --- integers, $A\ge0$; tidy for reading intercepts.
  \end{itemize}
  \vspace{5pt}
  \begin{block}{Why point-slope exists}
    In the phone plan the $y$-intercept was the one number nobody gave you. Point-slope lets you
    write the line \emph{immediately}, from the point you already have:
    \; $y-52 = 3(x-4)$.
  \end{block}
\end{frame}

% NOTES 3 — CONVERT
\begin{frame}[t, plain]
  \forestheader{Notes 3: same line, different outfit}
  \sectionlabel[redacc]{Expand, then tidy}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \textbf{Point-slope $\to$ slope-intercept}
      \begin{align*}
        y-52 &= 3(x-4) \\
        y-52 &= 3x-12 \\
           y &= 3x+40
      \end{align*}
      And there is the \$40 monthly fee.
    \end{column}
    \begin{column}{0.48\textwidth}
      \textbf{$\to$ standard}
      \begin{align*}
             y &= 3x+40 \\
        -3x+y &= 40 \\
         3x-y &= -40
      \end{align*}
      Integers, and $A \ge 0$.
    \end{column}
  \end{columns}
  \vspace{4pt}
  \textbf{Check any conversion:} at $x=10$, slope-intercept gives \$70 --- and so did the bill.
\end{frame}

% NOTES 4 — PARENT & SPECIAL CASES
\begin{frame}[t, plain]
  \forestheader{Notes 4: the parent $y=x$, and four special cases}
  \sectionlabel{And your turn}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \begin{itemize}\setlength{\itemsep}{3pt}
        \item Multiply by $k$: changes the \textbf{slope} (steeper, flatter, flipped).
        \item Add $k$: changes the \textbf{$y$-intercept}.
        \item \textbf{Parallel}: equal slopes. \; \textbf{Perpendicular}: opposite reciprocals,
              $m_1m_2=-1$.
        \item $y=c$: slope $0$. \; $x=a$: slope \emph{undefined} --- the one line $y=mx+b$ cannot
              write.
      \end{itemize}
    \end{column}
    \begin{column}{0.44\textwidth}
      \centering
      \begin{tikzpicture}[scale=0.38]
        \lgrid{-4}{5}{-6}{6}
        \draw[very thick, navy, dashed] (-4,-4)--(5,5) node[above left, font=\tiny, navy]{$y=x$};
        \draw[very thick, forest] (-1,-6)--(5,6) node[below right, font=\tiny, forest]{$y=2x-4$};
        \fill[charcoal] (0,-4) circle (4pt);
      \end{tikzpicture}
    \end{column}
  \end{columns}
  \vspace{3pt}
  \textbf{Your turn:} slope $-\tfrac12$ through $(2,-5)$ --- point-slope, then slope-intercept, then
  the parallel and perpendicular slopes.
\end{frame}

% GROUP ACTIVITY LAUNCH
\begin{frame}[t, plain]
  \forestheader{Group Activity: \emph{Two Receipts}}
  \sectionlabel[goldacc]{Groups of 3--4 $\cdot$ 25 minutes}
  \begin{columns}[T]
    \begin{column}{0.58\textwidth}
      \begin{enumerate}\setlength{\itemsep}{4pt}
        \item \textbf{Summit Gym} --- they show you the graph. Read the fee, the rate, write the rule.
        \item \textbf{Basecamp Gym} --- all you get is two receipts: 3 climbs \$24, 7 climbs \$32.
              Work back to the fee. Then: is \emph{Priya's} rule the same line as yours?
        \item \textbf{Which is cheaper?} At 2 climbs? At 15? Where does the answer switch?
        \item \textbf{Model it} --- the drone descent, two points to all three forms.
      \end{enumerate}
    \end{column}
    \begin{column}{0.40\textwidth}
      \centering
      \begin{tikzpicture}[x=0.44cm, y=0.11cm]
        \gymgrid
        \draw[very thick, forest] (0,6)--(8,30);
        \fill[charcoal] (0,6) circle (3pt);
        \fill[charcoal] (4,18) circle (3pt);
      \end{tikzpicture}\\[2pt]
      {\scriptsize Summit Gym}
    \end{column}
  \end{columns}
  \vspace{2pt}
  \textbf{The rule:} for every number you write down, be ready to show me \emph{where it came from}.
\end{frame}

% DEBRIEF
\begin{frame}[t, plain]
  \forestheader{Debrief: the four things to take away}
  \sectionlabel[redacc]{10 minutes --- correct your own page as we go}
  \begin{enumerate}\setlength{\itemsep}{5pt}
    \item \textbf{2b, the backwards move.} Three climbs cost \$6, so the fee is \$18 --- the same
          reasoning as the phone plan's \$40.
    \item \textbf{2c: same line, different outfit.} Priya's $D-24=2(n-3)$ expands to $D=2n+18$.
          Point-slope exists because the intercept is the point you did \emph{not} have.
    \item \textbf{Part 3:} slopes $3$ and $2$ are different, so the lines are not parallel --- they
          cross exactly once, at $n=12$ (\$42). That is where the cheaper gym switches.
    \item \textbf{4c:} starting from $60$~m changes $b$, not $m$. The line \emph{shifts up}; it does
          not tilt.
  \end{enumerate}
\end{frame}

% CLOSE
\begin{frame}[t, plain]
  \forestheader{Close}
  \sectionlabel{What changed today}
  You walked in able to \emph{read} a line's features off a graph. You walk out able to \emph{write}
  the line yourself --- from a graph, from two points, or from a slope and any point at all --- and
  to recognise the same line wearing three different outfits.
  \vspace{5pt}
  \begin{block}{Homework}
    The \textbf{Homework} page in your packet --- due next class. Six problems plus an extension:
    slope from two points (watch the horizontal and vertical cases), one line written two ways, a
    fraction-heavy conversion, the parent and its two odd cousins, a candle model, and one
    multiple-choice item on perpendicular slopes.
  \end{block}
  \vspace{4pt}
  \textbf{Next:} Lesson 1.2 --- a rule that reaches the same value \emph{twice}.
\end{frame}

\end{document}
