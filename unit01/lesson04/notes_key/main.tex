%! Piecewise & Step Functions — Unit 1, Lesson 1.4 — Guided Notes (Answer Key)
% Mirrors notes/main.tex exactly; every \blank{} becomes an \ans{} and every work block is
% authored byte-identically, so blank and key paginate alike. (Answer Key)
% Gradual release (course-wide shape, 2026-09-01): the notes are the "I do / we do" block,
% 20 minutes, and the Individual Practice block that closes them is the "you do alone", 15 more.
% There is no group activity.
\documentclass[10pt]{article}
\usepackage{algebra2-article}
\usepackage{algebra2-key}   % pulls in -boxes; do NOT also load -boxes
% Coordinate grid: {xmin}{xmax}{ymin}{ymax}
\newcommand{\lgrid}[4]{%
  \draw[step=1, linegray!40, very thin] (#1,#3) grid (#2,#4);
  \draw[->, semithick, charcoal] (#1,0)--(#2,0) node[below right, font=\scriptsize]{$x$};
  \draw[->, semithick, charcoal] (0,#3)--(0,#4) node[left, font=\scriptsize]{$y$};}
% Months grid for the streaming service: x = months 0..7, one y-unit = $12.
\newcommand{\mgrid}{%
  \draw[step=1, linegray!40, very thin] (0,0) grid (7,4);
  \draw[->, semithick, charcoal] (0,0)--(7.6,0) node[below right, font=\scriptsize]{$m$};
  \draw[->, semithick, charcoal] (0,0)--(0,4.6) node[left, font=\scriptsize]{\$};
  \foreach \x in {1,...,7}{\node[below, font=\tiny, charcoal] at (\x,0){$\x$};}
  \foreach \y/\lab in {1/12,2/24,3/36,4/48}{\node[left, font=\tiny, charcoal] at (0,\y){$\lab$};}}
% Endpoint dots: {color}{x}{y}
\newcommand{\fdot}[3]{\fill[#1] (#2,#3) circle (3.2pt);}
\newcommand{\hdot}[3]{\draw[very thick, #1, fill=white] (#2,#3) circle (3.2pt);}
% Vocab answer for the key: bold forest term, then the filled definition on a write-line.
% The leading and trailing \par are required: \ansline ends with \dotfill but does NOT end the
% paragraph, so without them each term label gets pulled onto the previous answer's line.
\newcommand{\vocabans}[2]{%
  \par\noindent\textbf{\textcolor{forest}{#1:}}\\[1pt]\ansline{#2}\par}

\begin{document}

\pageheader{Unit 1, Lesson 1.4}{Guided Notes --- Answer Key}

\vspace{0.10in}

\begin{objectivebox}
\textbf{By the end of this lesson, I will be able to\dots}
\begin{itemize}\setlength{\itemsep}{2pt}
  \item read piecewise notation and \emph{evaluate} $f(x)$ by choosing the piece $x$ belongs to
        --- and say which piece owns a \emph{boundary};
  \item write $|x|$, and any V, as a \emph{two-piece} linear rule;
  \item read a piecewise graph with \emph{open} and \emph{closed} endpoints, and decide whether
        the pieces \emph{meet} or \emph{jump} at a boundary;
  \item evaluate and read the \emph{greatest-integer (step) function} $\lfloor x\rfloor$, and
        name constant intervals.
\end{itemize}
\end{objectivebox}

\vspace{0.05in}

\begin{vocabbox}
\small
Fill in each term as we build it together.
\par\vspace{2pt}   % \par is required: \vocabans's \noindent is a no-op mid-paragraph
\vocabans{Piecewise-defined function}{one function with different rules on different parts of its domain, written with a brace}
\vocabans{Boundary (breakpoint)}{an $x$-value where the rule changes; the piece whose inequality includes it owns it}
\vocabans{Open vs.\ closed endpoint}{open $\circ$ excludes the point ($<,>$); closed $\bullet$ includes it ($\le,\ge$)}
\vocabans{Continuous at a boundary / discontinuity (jump)}{pieces meet if both rules give the same value there; otherwise the graph jumps}
\vocabans{Greatest-integer function $\lfloor x\rfloor$}{the greatest integer $\le x$ --- rounds \emph{down}; $\lfloor-2.5\rfloor=-3$}
\vocabans{Step function}{a piecewise function that is constant on each interval, then jumps (a staircase)}
\end{vocabbox}

\begin{hookbox}
A streaming service is \emph{free for the first 3 months}, then costs \emph{\$12 a month} for
every month after that. Let $C(m)$ be the \emph{total} you have paid after $m$ months.
\begin{itemize}\setlength{\itemsep}{2pt}
  \item Total paid after $2$ months: \ans{$0$} \quad after $3$ months: \ans{$0$} \quad
        after $5$ months: \ans{$24$}
  \item Could one straight line describe the total for \emph{every} month? \ans{no} \;
        How many different rules does the cost follow? \ans{two}
\end{itemize}
One function, two rules --- one for the free months and another after them. That is a
\textbf{piecewise-defined function}, and today we learn to evaluate them, read their graphs, and
meet the step function that ``rounds.''
\end{hookbox}

\boxguard[24]
\begin{notesbox}{1. One Function, Several Rules --- Evaluate by Choosing the Piece}
A \textbf{piecewise-defined function} uses different rules on different parts of its domain,
written with a brace. Each rule comes with the inputs it is \emph{for}. For the service:
\[ C(m)=\begin{cases} 0, & 0\le m\le 3 \\ 12(m-3), & m>3 \end{cases} \]
\textbf{To evaluate: first pick the piece, then apply its rule.}
\begin{itemize}[leftmargin=1.5em, itemsep=2pt]
  \item $C(2)$: is $2\le3$? \ans{yes} \; so use the \ans{first} piece: $C(2)=$ \ans{$0$}
  \item $C(3)$: the boundary. $3$ satisfies $0\le m\le3$ (the $\le$ includes it), so $C(3)=$
        \ans{$0$} --- month $3$ is still free.
  \item $C(5)$: is $5>3$? \ans{yes} \; so use the second piece:
        \begin{work}
          C(5) &= 12(5-3) \\
               &= 12(2) \\
               &= 24
        \end{work}
\end{itemize}
\textbf{Boundary rule:} an input on the boundary belongs to the piece whose inequality
\emph{includes} it --- the one with $\le$ or $\ge$, never the strict one. \\[2pt]
\textbf{Try one with no story.} $f(x)=\begin{cases} 2x+1, & x<2 \\ 5-x, & x\ge2 \end{cases}$
\quad $f(0)=$ \ans{$1$} \quad $f(2)=$ \ans{$3$} (which piece? \ans{second}) \quad
$f(5)=$ \ans{$0$}
\end{notesbox}

\boxguard[24]
\begin{notesbox}{2. Absolute Value Is Piecewise --- the V Is Two Pieces}
Lesson 1.3's V is two lines pasted at the vertex, and now we can \emph{write} it that way. When
$x\ge0$, the bars change nothing: $|x|=x$. When $x<0$, the bars flip the sign: $|x|=-x$.
\[ |x|=\begin{cases} \text{\ans{$x$}}, & x\ge0 \\[2pt] \text{\ans{$-x$}}, & x<0 \end{cases} \]
\vspace{-4pt}
\begin{center}Check: $|-3| = -(-3) =$ \ans{$3$}\end{center}
\textbf{Worked: the trail from Lesson 1.3,} $H(x)=2|x-3|+1$. The vertex $x=3$ is the boundary.
\begin{center}
\begin{minipage}[t]{0.47\linewidth}
For $x\ge3$, $|x-3|=x-3$:
\begin{work}
  H(x) &= 2(x-3) + 1 \\
       &= 2x - 6 + 1 \\
       &= 2x - 5
\end{work}
\end{minipage}\hfill
\begin{minipage}[t]{0.47\linewidth}
For $x<3$, $|x-3|=-(x-3)=3-x$:
\begin{work}
  H(x) &= 2(3-x) + 1 \\
       &= 6 - 2x + 1 \\
       &= 7 - 2x
\end{work}
\end{minipage}
\end{center}
So $H(x)=\begin{cases} 2x-5, & x\ge3 \\ 7-2x, & x<3 \end{cases}$ --- the right ray has slope
\ans{$2$} and the left ray has slope \ans{$-2$}, and at the boundary both rules give
$H(3)=$ \ans{$1$}, so the two pieces \ans{meet} (meet / jump) at the vertex.
\end{notesbox}

\boxguard[28]
\begin{notesbox}{3. Reading a Graph: Open or Closed, Meets or Jumps}
On a graph, a \textbf{closed} dot $\bullet$ \emph{includes} its endpoint ($\le,\ge$) and an
\textbf{open} dot $\circ$ \emph{excludes} it ($<,>$). Same story, two different functions --- the
\emph{total} paid so far, and the \emph{price this month}:
\[ C(m)=\begin{cases} 0, & 0\le m\le3 \\ 12(m-3), & m>3 \end{cases} \qquad\qquad
   P(m)=\begin{cases} 0, & 0\le m\le3 \\ 12, & m>3 \end{cases} \]
\vspace{-6pt}
\begin{center}
\renewcommand{\arraystretch}{1.35}
\begin{tabularx}{\linewidth}{@{}X|X@{}}
\textbf{Total paid $C(m)$ --- meets} & \textbf{Price this month $P(m)$ --- jumps} \\
\hline
\centering\arraybackslash
\begin{tikzpicture}[scale=0.36, baseline=(current bounding box.center)]
  \mgrid
  \draw[very thick, forest] (0,0)--(3,0);
  \draw[very thick, forest] (3,0)--(7,4);
  \fdot{forest}{3}{0}
\end{tikzpicture}
&
\centering\arraybackslash
\begin{tikzpicture}[scale=0.36, baseline=(current bounding box.center)]
  \mgrid
  \draw[very thick, redacc] (0,0)--(3,0);
  \draw[very thick, redacc] (3,1)--(7,1);
  \fdot{redacc}{3}{0}
  \hdot{redacc}{3}{1}
\end{tikzpicture}
\\
At $m=3$: first rule gives \ans{$0$}; second rule (just past $3$) gives \ans{$0$} &
At $m=3$: first rule gives \ans{$0$}; second rule (just past $3$) gives \ans{$12$} \\
Same value? \ans{yes} \; So the pieces \ans{meet} (meet / jump) & Same value? \ans{no} \; So the pieces \ans{jump} (meet / jump) \\
\emph{Continuous} at $m=3$; constant on \ans{$[0,3]$}, increasing on \ans{$[3,\infty)$} & A \emph{discontinuity} at $m=3$; the range is just \ans{$\{0,12\}$} \\
\end{tabularx}
\end{center}
\vspace{2pt}
\begin{tcolorbox}[colback=goldbg, colframe=goldacc, boxrule=0.9pt, arc=2mm,
    left=2.5mm, right=2.5mm, top=1.5mm, bottom=1.5mm]
\small \textbf{Careful --- at a boundary, exactly one piece owns the input.} A classmate reads
$P(3)=12$ ``because the price is \$12 after the free months.'' But $3$ satisfies $0\le m\le3$, so
$P(3)=$ \ans{$0$}: month $3$ is still free, and the \ans{closed} (open / closed) dot at
$(3,0)$ says so. The open dot at $(3,12)$ is \emph{not} a point of the graph --- it marks where
the second piece \emph{starts without} its endpoint.
\end{tcolorbox}
\end{notesbox}

\boxguard[26]
\begin{notesbox}{4. Step Functions: the Greatest-Integer Function $\lfloor x\rfloor$}
The monthly price $P(m)$ is \emph{constant} on each piece, then jumps --- a \textbf{step
function}. The most famous one is the \textbf{greatest-integer function} $\lfloor x\rfloor$: the
greatest integer that is \emph{less than or equal to} $x$. It \textbf{rounds down}.
\begin{center}
\begin{minipage}[c]{0.56\linewidth}
$\lfloor 3.7\rfloor=$ \ans{$3$} \quad $\lfloor 5\rfloor=$ \ans{$5$} \quad
$\lfloor -0.4\rfloor=$ \ans{$-1$} \quad $\lfloor -2.5\rfloor=$ \ans{$-3$} \\[4pt]
\emph{Careful:} rounding \emph{down} sends $-2.5$ to \ans{$-3$}, not $-2$ --- down means
\emph{more negative}, not ``toward zero.'' \\[4pt]
The graph is a \textbf{staircase}: on each interval $[n,n+1)$ the output is the constant $n$
--- closed on the left, open on the right --- then it jumps up by $1$. \\[4pt]
From the staircase: $\lfloor 1.5\rfloor=$ \ans{$1$}; \; $\lfloor x\rfloor=2$ for every $x$ in
\ans{$[2,3)$}; \; the function is \ans{constant} (increasing / constant) on that interval.
\end{minipage}\hfill
\begin{minipage}[c]{0.40\linewidth}\centering
\begin{tikzpicture}[scale=0.40]
  \lgrid{-1.5}{4.5}{-1.5}{4}
  \draw[very thick, forest] (-1,-1)--(0,-1); \fdot{forest}{-1}{-1} \hdot{forest}{0}{-1}
  \draw[very thick, forest] (0,0)--(1,0);    \fdot{forest}{0}{0}   \hdot{forest}{1}{0}
  \draw[very thick, forest] (1,1)--(2,1);    \fdot{forest}{1}{1}   \hdot{forest}{2}{1}
  \draw[very thick, forest] (2,2)--(3,2);    \fdot{forest}{2}{2}   \hdot{forest}{3}{2}
  \draw[very thick, forest] (3,3)--(4,3);    \fdot{forest}{3}{3}   \hdot{forest}{4}{3}
\end{tikzpicture}
\end{minipage}
\end{center}
Real quantities that ``round down'' are step functions: your age in whole years is
$\lfloor\text{exact age}\rfloor$, and the number of full hours a car has been parked is
$\lfloor t\rfloor$.
\end{notesbox}

\boxguard[24]
\begin{practicebox}
For $p(x)=\begin{cases} -2x, & x\le -1 \\ x+3, & x>-1 \end{cases}$, evaluate, decide, then read.
\begin{center}
\begin{minipage}[c]{0.60\linewidth}
\begin{enumerate}[leftmargin=1.7em, itemsep=3pt]
  \item $p(-3)=$ \ans{$6$} \quad $p(-1)=$ \ans{$2$} (which piece? \ans{first}) \\[2pt]
        $p(0)=$ \ans{$3$} \quad $p(2)=$ \ans{$5$}
  \item \textbf{Meets or jumps at $x=-1$?} The first rule gives \ans{$2$} and the second, just
        past $-1$, gives \ans{$2$}. Same value, so the graph \ans{meets} (meets / jumps):
        $p$ \emph{is} continuous at $x=-1$.
  \item From the graph: decreasing on \ans{$(-\infty,-1]$}, increasing on \ans{$[-1,\infty)$}; the minimum
        value is \ans{$2$}, at the boundary.
\end{enumerate}
\end{minipage}\hfill
\begin{minipage}[c]{0.36\linewidth}\centering
\begin{tikzpicture}[scale=0.30]
  \lgrid{-4.5}{4.5}{-1}{8}
  \draw[very thick, forest] (-4,8)--(-1,2);
  \draw[very thick, forest] (-1,2)--(4,7);
  \fdot{forest}{-1}{2}
\end{tikzpicture}
\end{minipage}
\end{center}
\end{practicebox}

\boxguard[22]
\begin{scenariobox}[Individual Practice --- On Your Own]{navy}
Three problems, quietly and on your own. First find the piece, then apply the rule --- and at a
boundary, ask which inequality \emph{includes} the input.
\begin{enumerate}[leftmargin=1.9em, itemsep=9pt]
  \item \textbf{Across the boundary.} $h(x)=\begin{cases} -x+1, & x\le2 \\ 2x-6, & x>2 \end{cases}$
    \\[3pt]
    $h(0)=$ \ans{$1$} \quad $h(2)=$ \ans{$-1$} (which piece owns $x=2$? \ans{first ($\le$)})
    \quad $h(5)=$ \ans{$4$} \\[3pt]
    At $x=2$ the first rule gives \ans{$-1$} and the second gives \ans{$-2$}, so the graph
    \ans{jumps} (meets / jumps) there: $h$ is \ans{not} continuous at $x=2$.

  \item \textbf{From the graph.} The graph shows \emph{all} of $f$. \\[2pt]
    \begin{minipage}[c]{0.60\linewidth}
    Domain \ans{$[-2,5]$}; \; $f(1)=$ \ans{$1$}; \; $f(2)=$ \ans{$-2$} (read the
    \ans{closed} dot). \\[3pt]
    Increasing on \ans{$[-2,2)$}; \; constant on \ans{$[2,5]$}. \\[3pt]
    Continuous at $x=2$? \ans{no} \; Now write its rule: \\[2pt]
    $f(x)=\begin{cases} \text{\ans{$x$}}, & -2\le x<2 \\[2pt] \text{\ans{$-2$}}, & 2\le x\le5 \end{cases}$
    \end{minipage}\hfill
    \begin{minipage}[c]{0.36\linewidth}\centering
    \begin{tikzpicture}[scale=0.36]
      \lgrid{-2.5}{5.5}{-3}{3}
      \draw[very thick, forest] (-2,-2)--(2,2);
      \fdot{forest}{-2}{-2} \hdot{forest}{2}{2}
      \draw[very thick, forest] (2,-2)--(5,-2);
      \fdot{forest}{2}{-2} \fdot{forest}{5}{-2}
    \end{tikzpicture}
    \end{minipage}

  \item \textbf{Two pieces and the staircase.} Write $|x-3|$ as two pieces, then round down.
    \begin{center}
    \begin{minipage}[t]{0.47\linewidth}
    For $x\ge3$:
    \begin{work}
      |x-3| &= x - 3
    \end{work}
    \end{minipage}\hfill
    \begin{minipage}[t]{0.47\linewidth}
    For $x<3$:
    \begin{work}
      |x-3| &= -(x-3) \\
            &= 3 - x
    \end{work}
    \end{minipage}
    \end{center}
    $\lfloor 2.9\rfloor=$ \ans{$2$} \quad $\lfloor -1.2\rfloor=$ \ans{$-2$} \quad
    $\lfloor 4\rfloor=$ \ans{$4$} \quad $\lfloor x\rfloor=4$ for every $x$ in \ans{$[4,5)$}
\end{enumerate}
\end{scenariobox}

\end{document}
