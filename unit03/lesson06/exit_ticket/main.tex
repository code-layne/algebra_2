%! Graphing Polynomial Functions and Modeling — Unit 3, Lesson 3.6 — Exit Ticket
\documentclass[10pt]{article}
\usepackage{algebra2-article}
\usepackage{algebra2-boxes}
\newcommand{\TallMath}[1]{$\displaystyle #1\rule[-1.4em]{0pt}{3.2em}$}

\newcommand{\lgrid}[4]{%
  \draw[step=1, linegray!40, very thin] (#1,#3) grid (#2,#4);
  \draw[->, semithick, charcoal] (#1,0)--(#2,0) node[below right, font=\scriptsize]{$x$};
  \draw[->, semithick, charcoal] (0,#3)--(0,#4) node[left, font=\scriptsize]{$y$};}

\begin{document}

\pageheader{Unit 3, Lesson 3.6}{Exit Ticket}
\namedateperiod

\vspace{0.08in}

No notes. Predict from the equation; read from the picture. Both directions count.

\begin{enumerate}[leftmargin=1.8em, itemsep=10pt]
  \item \textbf{Predict the graph.} $f(x) = (x-3)(x+1)^2$

    \vspace{3pt}
    (a) Degree \blank{0.8cm}, leading coefficient \blank{0.8cm}. \; As $x \to -\infty$, $f(x) \to$
    \blank{1.2cm}; as $x \to +\infty$, $f(x) \to$ \blank{1.2cm}

    \vspace{3pt}
    (b) Zeros and behavior: $x = $ \blank{1.0cm} (the graph \blank{1.8cm}), \quad
    $x = $ \blank{1.0cm} (the graph \blank{1.8cm}).

    \vspace{3pt}
    (c) $y$-intercept: \blank{1.6cm} \qquad At most how many turning points? \blank{1.0cm}

    \vspace{3pt}
    (d) Test $x=0$: $f(0)$ is \blank{1.6cm} (positive/negative), so on $-1<x<3$ the graph is
    \blank{1.6cm} the $x$-axis. Is it above or below just to the \emph{left} of $x=-1$?
    \blank{1.6cm} \; Why does that not change at $x=-1$? \blank{4.4cm}

  \item \textbf{Read the picture.} The graph below is a cubic; each horizontal gridline is $4$ units.
    \begin{center}
    \begin{tikzpicture}[scale=0.45]
      \lgrid{-4}{4}{-5}{2}
      \begin{scope}
        \clip (-4,-5) rectangle (4,2);
        \draw[very thick, forest, domain=-3.3:3.1, samples=170, smooth]
          plot (\x,{(0 - (\x)*(\x)*(\x) + (\x)*(\x) + 8*(\x) - 12)/4});
      \end{scope}
      \foreach \x in {-3,-2,-1,1,2,3}
        \draw[charcoal] (\x,-0.15)--(\x,0.15) node[above=1pt, font=\tiny]{$\x$};
      \fill[charcoal] (-3,0) circle (2.5pt);
      \fill[charcoal] (2,0)  circle (2.5pt);
    \end{tikzpicture}
    \end{center}

    \vspace{2pt}
    (a) $x$-intercepts: \blank{2.4cm} \; Which is a touch? \blank{1.2cm} \;
    $y$-intercept: \blank{1.4cm}

    \vspace{3pt}
    (b) Skeleton: $f(x) = a\,($\blank{1.6cm}$)($\blank{1.6cm}$)^2$ \quad Solve for $a$:
    $a = $ \blank{1.2cm}

    \vspace{3pt}
    (c) A possible equation: \blank{5.0cm}

    \vspace{3pt}
    (d) Does this function have an absolute maximum? \blank{1.2cm} \; Explain in one sentence.
    \blank{5.6cm}

  \item \textbf{SOL-style multiple choice.} Which function's graph \emph{touches} the $x$-axis at
    $x=2$ and falls toward $-\infty$ on \emph{both} ends?
    \begin{enumerate}[label=\Alph*., leftmargin=1.9em, itemsep=1pt]
      \item $f(x) = -(x-2)^2(x+1)^2$
      \item $f(x) = -(x-2)(x+1)^2$
      \item $f(x) = (x-2)^2(x+1)^2$
      \item $f(x) = -(x-2)^3(x+1)$
    \end{enumerate}
\end{enumerate}

\end{document}
