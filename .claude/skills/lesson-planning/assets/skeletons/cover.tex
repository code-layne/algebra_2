%! @@TITLE@@ — Unit @@UNITINT@@, Lesson @@LESSONID@@ — Student Packet Cover Sheet
\documentclass[10pt]{article}
\usepackage{@@PREFIX@@-article}
\usepackage{@@PREFIX@@-boxes}
\usepackage{ltablex}
\keepXColumns

\begin{document}

% Full-bleed forest banner
% The header text is set INSIDE a tikz node anchored to the page so it can never
% drift above the paper edge (a negative \vspace here pushes it off the sheet).
\begin{tikzpicture}[remember picture, overlay]
  \fill[forest] (current page.north west)
    rectangle ([yshift=-1.16in]current page.north east);
  \node[anchor=north, align=center, inner sep=0pt, text width=\paperwidth]
    at ([yshift=-0.28in]current page.north) {%
      {\color{white}\bfseries\LARGE @@COURSENAME@@}\\[4pt]
      {\color{forestmid}\bfseries\large Unit @@UNITINT@@}\\[6pt]
      {\color{white}\bfseries\large Lesson @@LESSONID@@ \quad @@TITLE@@}%
    };
\end{tikzpicture}

% Push the body clear of the 1.16in banner (page top margin is 0.60in).
\vspace*{0.56in}
\namedateperiod
\vspace{0.12in}

% Learning targets use the lesson's FORMAL VOCABULARY. (The EFFL spoiler rule is dead: the
% Guided Notes name every term before the practice uses it.)
\begin{learningtargetbox}
\small
\begin{enumerate}[leftmargin=1.5em, itemsep=5pt]
  \item \ldots{} TODO: learning target 1, naming the vocabulary in \textbf{bold}.
  \item \ldots{} TODO: learning target 2.
\end{enumerate}
\end{learningtargetbox}

\vspace{0.14in}

\begin{tocbox}
\small
\renewcommand{\arraystretch}{1.5}
\begin{tabularx}{\linewidth}{@{} c l X r @{}}
\rowcolor{forestbg}
\textbf{\#} & \textbf{Component} & \textbf{Description} & \textbf{Score} \\
\midrule
% Rows are in packet order (shared/lesson.mk STUDENT_ORDER). THREE scored rows: there is no
% group activity (cut course-wide 2026-09-01 — the "you do" is the Individual Practice block
% inside the Guided Notes), the DEBRIEF has no row (it is a lesson phase, not a component), and
% there is no exit ticket. EVERY row is scored, homework included — a \blank{1.2cm}, never NA.
% Four columns (c l X r): every row needs FOUR cells or the column widths collapse.
1 & Warm-Up        & TODO: spiral review --- the prior skills today leans on   & \blank{1.2cm} \\
2 & Guided Notes   & TODO: the four sections, named --- ending in \emph{Guided Practice} and
                     \emph{Individual Practice}                                & \blank{1.2cm} \\
3 & Homework       & TODO: what the practice covers --- \emph{due next class}  & \blank{1.2cm} \\
\midrule
  & \multicolumn{2}{r}{\textbf{Total}} & \blank{1.2cm} \\
\end{tabularx}
\end{tocbox}

\vspace{0.10in}

% Keep in Mind is a CONTENT summary — the lesson's definitions and the distinction it turns on,
% in a form the student can revise from. (Under EFFL this box described the process instead.)
\begin{remindbox}
\small TODO: the lesson's key definitions and its target distinction, in three or four sentences.
\end{remindbox}

\end{document}
