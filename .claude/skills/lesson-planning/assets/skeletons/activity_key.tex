%! @@TITLE@@ — Unit @@UNITINT@@, Lesson @@LESSONID@@ — Group Activity (Answer Key)
% Mirrors activity/main.tex. Each \writelines{n} is answered with EXACTLY n \ansline's, and every
% work block is authored byte-identically, so the two files come out the same number of pages.
\documentclass[10pt]{article}
\usepackage{@@PREFIX@@-article}
\usepackage{@@PREFIX@@-key}   % pulls in -boxes; do NOT also load -boxes
% Coordinate grid: {xmin}{xmax}{ymin}{ymax}
\newcommand{\lgrid}[4]{%
  \draw[step=1, linegray!40, very thin] (#1,#3) grid (#2,#4);
  \draw[->, semithick, charcoal] (#1,0)--(#2,0) node[below right, font=\scriptsize]{$x$};
  \draw[->, semithick, charcoal] (0,#3)--(0,#4) node[left, font=\scriptsize]{$y$};}

\begin{document}

\pageheader{Unit @@UNITINT@@, Lesson @@LESSONID@@}{Group Activity --- Answer Key}
% NAMESTRIP: no \namedateperiod here — mirrors the blank.

\vspace{0.08in}

% TODO: copy activity/main.tex verbatim from here down, then fill every blank.
%   \blank{2.0cm}   ->  \ans{answer}       (short — a wide \ans can wrap and shift a page break)
%   \writelines{2}  ->  two \ansline{...}  (one per reserved line; keep each under ~95 characters
%                                           so it does not wrap onto a third)
%   \begin{work} ... \end{work}  ->  UNCHANGED, byte for byte
\begin{headlinebox}{forestbg}
\small\textbf{TODO Activity Title.} TODO --- identical to the blank.
\end{headlinebox}

\end{document}
